<%= form_for @vrapp do |f| %>
	<% if @vrapp.errors.any? %>
		<h3><%= pluralize(@vrapp.errors.count, "error") %> prevented this App from saving:</h3>
		<ul>
			<% @vrapp.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	<% end %>

	<%= f.label :name %><br>
	<%= f.text_field :name %>

	<br>
	<br>

  <%= f.label :description %><br>
	<%= f.text_area :description %>

	<br>
	<br>

	<%= f.label :tag %><br>
	<%= f.text_field :tag %>

	<br>
	<br>

	<%= f.label :thumbnail %><br>
	<%= f.file_field :thumbnail %>

	<br>
	<br>

	<%= f.submit "Save", class: "button button_highlight button_block" %>
<% end %>